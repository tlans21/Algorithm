#include <string>
#include <vector>
#include <stack>
using namespace std;

vector<int> solution(vector<int> prices) {
    vector<int> answer;
    stack<int> s;
    
    
    for(int i = 0; prices.size(); i++){
        while(!s.empty() && prices[])
    }
    return answer;
}